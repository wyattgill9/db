[package]
name = "types"
version = "0.1.0"
edition = "2024"
repository = "https://github.com/wyattgill9/db"
license = "MIT"

[dependencies]
bytes = { workspace = true }
zerocopy = { workspace = true }
